import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["macbookwail.local", "10.88.111.36"],
};

export default nextConfig;
